{
  "name": "fhevm-counter",
  "version": "1.0.0",
  "description": "FHEVM Confidential Counter on Zama Sepolia",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@fhenixprotocol/contracts": "^0.0.1",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.0.0",
    "hardhat": "^2.20.0"
  }
}
